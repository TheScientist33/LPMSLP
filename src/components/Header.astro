---
import { localizeUrl, type Locale, type RouteKey } from '~/i18n/ui';
import { useTranslations } from '~/i18n/t';
import { site } from '~/data/site';
import LanguageSwitcher from './LanguageSwitcher.astro';

interface Props {
  locale: Locale;
}
const { locale } = Astro.props;
const t = useTranslations(locale);

const navItems: { key: RouteKey; label: string }[] = [
  { key: 'home', label: t('nav.home') },
  { key: 'house', label: t('nav.house') },
  { key: 'pool', label: t('nav.pool') },
  { key: 'rates', label: t('nav.rates') },
  { key: 'discover', label: t('nav.discover') },
  { key: 'reviews', label: t('nav.reviews') },
  { key: 'contact', label: t('nav.contact') },
];

const current = Astro.url.pathname.replace(/\/$/, '');
const isActive = (key: RouteKey) => current === localizeUrl(locale, key).replace(/\/$/, '');
---

<header class="border-beige bg-cream/95 sticky top-0 z-40 border-b backdrop-blur">
  <div
    class="mx-auto flex max-w-[1440px] items-center justify-between gap-4 px-5 py-4 lg:px-8 md:py-[22px] xl:px-14"
  >
    <a
      href={localizeUrl(locale, 'home')}
      class="font-display text-ink text-base leading-tight font-semibold no-underline lg:text-lg xl:text-[22px]"
    >
      La petite maison sous les <span class="text-pine">pins</span>
    </a>

    <nav aria-label={t('nav.menu')} class="hidden items-center gap-4 lg:flex xl:gap-[26px]">
      {
        navItems.map((item) => (
          <a
            href={localizeUrl(locale, item.key)}
            aria-current={isActive(item.key) ? 'page' : undefined}
            class:list={[
              'text-sm font-medium no-underline transition-colors xl:text-[15px]',
              isActive(item.key) ? 'text-pine font-semibold' : 'text-ink hover:text-marine',
            ]}
          >
            {item.label}
          </a>
        ))
      }
    </nav>

    <div class="hidden items-center gap-3 lg:flex xl:gap-[18px]">
      <LanguageSwitcher locale={locale} />
      <a
        href={localizeUrl(locale, 'rates')}
        class="bg-pine text-cream hover:bg-pine/90 hidden items-center rounded-[5px] px-[18px] py-2.5 text-sm font-medium no-underline transition-colors xl:inline-flex"
      >
        {t('common.availability')}
      </a>
    </div>

    <!-- Mobile -->
    <div class="flex items-center gap-3 lg:hidden">
      <a
        href={`tel:${site.phoneMobile}`}
        aria-label={t('nav.call')}
        class="border-pine text-pine inline-flex h-10 w-10 items-center justify-center rounded-lg border-[1.5px]"
      >
        <svg width="18" height="18" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
          <path
            d="M3.5 3h3l1.2 3.2-1.6 1.1a10 10 0 004.6 4.6l1.1-1.6L16 14.5v3a1.5 1.5 0 01-1.6 1.5A13.5 13.5 0 012 4.6 1.5 1.5 0 013.5 3z"
          ></path>
        </svg>
      </a>
      <button
        type="button"
        id="menu-toggle"
        aria-expanded="false"
        aria-controls="mobile-menu"
        class="text-ink inline-flex h-10 w-10 flex-col items-center justify-center gap-[5px] rounded-lg"
      >
        <span class="sr-only">{t('nav.menu')}</span>
        <span aria-hidden="true" class="bg-ink block h-0.5 w-[22px]"></span>
        <span aria-hidden="true" class="bg-ink block h-0.5 w-[22px]"></span>
        <span aria-hidden="true" class="bg-ink block h-0.5 w-[22px]"></span>
      </button>
    </div>
  </div>

  <div id="mobile-menu" hidden class="border-beige border-t lg:hidden">
    <nav aria-label={t('nav.menu')} class="mx-auto flex max-w-[1440px] flex-col px-5 py-2">
      {
        navItems.map((item) => (
          <a
            href={localizeUrl(locale, item.key)}
            aria-current={isActive(item.key) ? 'page' : undefined}
            class:list={[
              'border-beige/60 border-b py-3 no-underline last:border-0',
              isActive(item.key) ? 'text-pine font-semibold' : 'text-ink',
            ]}
          >
            {item.label}
          </a>
        ))
      }
      <div class="flex items-center justify-between py-3">
        <LanguageSwitcher locale={locale} />
        <a
          href={localizeUrl(locale, 'rates')}
          class="bg-pine text-cream inline-flex items-center rounded-[5px] px-4 py-2 text-sm font-medium no-underline"
        >
          {t('common.availability')}
        </a>
      </div>
    </nav>
  </div>
</header>

<script>
  const toggle = document.getElementById('menu-toggle');
  const menu = document.getElementById('mobile-menu');
  toggle?.addEventListener('click', () => {
    const open = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!open));
    if (menu) menu.hidden = open;
  });
</script>
